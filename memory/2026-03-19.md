# 2026-03-19

## 安全审计 (02:00 AM)

`openclaw security audit --deep` 结果：**10 critical · 8 warn · 2 info**

### Critical (10)

1. **plugins.allow 未设置** — extensions 目录有 1 个插件(lossless-claw)但没有白名单，任何插件都可能自动加载
2. **9 个 baoyu-* skill 包含危险代码模式**：
   - `baoyu-compress-image` — shell exec (1处)
   - `baoyu-danger-gemini-web` — shell exec + env harvesting (3处)
   - `baoyu-danger-x-to-markdown` — env harvesting + shell exec (5处)
   - `baoyu-format-markdown` — shell exec (1处)
   - `baoyu-image-gen` — env harvesting + shell exec (8处)
   - `baoyu-post-to-wechat` — shell exec + env harvesting (10处)
   - `baoyu-post-to-weibo` — shell exec + env harvesting (5处)
   - `baoyu-post-to-x` — shell exec + env harvesting (6处)
   - `baoyu-url-to-markdown` — shell exec + env harvesting (2处)

### Warn (8)

- trusted_proxies 未配置（反向代理场景下有 header spoofing 风险）
- nodes.denyCommands 有无效条目（camera.snap/clip, screen.record 等命名不匹配）
- anthropic-proxy/claude-haiku-4-5 在 fallback 中，tier 偏低
- 多用户场景检测：Discord allowlist + 无沙箱的 exec/fs 工具暴露
- 插件工具在宽松策略下可达
- lossless-claw 未 pin 版本号
- lossless-claw 有可疑的文件读取+网络发送模式
- gateway deep probe 失败（缺少 operator.read scope）

### Info (2)

- 攻击面摘要：2个 allowlist 群组，elevated tools 开启，browser control 开启
- HTTP API session-key override 已启用

### 建议优先处理

1. **设置 plugins.allow** 白名单（最快降低风险）
2. **审查 baoyu-* skills** 的 env-harvesting 模式，确认是否为预期行为
3. **Pin lossless-claw 版本号**
4. **修正 nodes.denyCommands** 无效条目
